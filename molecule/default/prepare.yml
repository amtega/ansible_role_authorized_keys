---

- name: Prepare
  hosts: molecule_hosts
  gather_facts: no
  tasks:
    - name: Setup packages
      package:
        name: openssh
        state: present

    - name: Setup testing user
      user:
        name: testuser
        comment: Testing user
